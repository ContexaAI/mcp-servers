{
  "version": "1.1.0",
  "description": "Hyperbrowser Model Context Protocol Server",
  "name": "hyperbrowser-mcp",
  "main": "./dist/server.js",
  "bin": {
    "hyperbrowser-mcp": "dist/server.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "if exist dist rmdir /s /q dist && tsc",
    "start": "node dist/server.js",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "format": "prettier --write .",
    "build-resources": "node --loader ts-node/esm scripts/build_resources.ts && copy summarized.json src\\resources\\static\\data\\summarized.json && del summarized.json && del static_data.json",
    "prepare": "npm run build",
    "publish": "if exist dist rmdir /s /q dist && if exist node_modules rmdir /s /q node_modules && npm i && npm run build && npm publish"
  },
  "dependencies": {
    "@hyperbrowser/sdk": "^0.48.1",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ajv": "^8.17.1",
    "axios": "^1.8.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^16.11.7",
    "openai": "^4.87.3",
    "ts-node": "^10.9.2",
    "typescript": "^4.5.2"
  },
  "keywords": [
    "mcp",
    "hyperbrowser",
    "web-scraping",
    "crawler",
    "content-extraction"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hyperbrowserai/mcp.git"
  },
  "types": "./dist/server.d.ts",
  "bugs": {
    "url": "https://github.com/hyperbrowserai/mcp/issues"
  },
  "homepage": "https://github.com/hyperbrowserai/mcp#readme",
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "ISC"
}
